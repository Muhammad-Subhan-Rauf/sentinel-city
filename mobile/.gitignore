node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store
.env.local
expo-env.d.ts

# Native projects are generated by `expo prebuild` — don't commit them.
# The source of truth is app.json + the plugin list + native deps.
android/
ios/
